import { defineApp } from "convex/server";

const app = defineApp();

export default app;

// Note: We're using Mastra Agent instead of Convex Agent component
// No component registration needed - agent is created directly in agent.ts

